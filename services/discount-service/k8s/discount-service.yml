apiVersion: v1
kind: Service
metadata:
  name: discount-service
  namespace: discount-service
spec:
  selector:
    app: discount-service
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8085
  type: ClusterIP