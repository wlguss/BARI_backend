apiVersion: v1
kind: Service
metadata:
  name: store-service
  namespace: store
spec:
  selector:
    app: store-service
  ports:
    - protocol: TCP
      port: 8082        
      targetPort: 8082  
  type: ClusterIP