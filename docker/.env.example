# docker-compose 인프라(MariaDB, RabbitMQ) 로컬 설정 템플릿
# 사용법: cp docker/.env.example docker/.env  후 값 채우기
# docker/.env는 .gitignore에 의해 git에 커밋되지 않는다.

MYSQL_ROOT_PASSWORD=root1234
MYSQL_DATABASE=bari
MYSQL_USER=bari

RABBITMQ_DEFAULT_USER=bari
RABBITMQ_DEFAULT_PASS=bari1234
